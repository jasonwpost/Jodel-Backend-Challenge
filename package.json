{
  "name": "jodelchallenge",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "REDIS_HOST=redis MONGO_HOST=mongodb node ./bin/www",
    "local": "REDIS_HOST=localhost MONGO_HOST=localhost node ./bin/www",
    "test": "jest"
  },
  "dependencies": {
    "bluebird": "^3.5.1",
    "body-parser": "~1.18.2",
    "express": "~4.15.5",
    "mongoose": "^5.0.1",
    "mongoose-paginate": "^5.0.3",
    "morgan": "^1.9.0",
    "redis": "^2.8.0"
  },
  "devDependencies": {
    "jest": "^22.1.4",
    "supertest": "^3.0.0"
  }
}
